{
  "name": "campusos",
  "version": "0.0.0",
  "private": true,
  "description": "CampusOS — The School Operating System",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "test:ci": "turbo test:ci",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "db:migrate": "pnpm --filter @campusos/database migrate",
    "db:seed": "pnpm --filter @campusos/database seed",
    "db:seed:all": "pnpm --filter @campusos/database seed && pnpm --filter @campusos/database seed:sis && pnpm --filter @campusos/database seed:hr && pnpm --filter @campusos/database seed:classroom && pnpm --filter @campusos/database seed:scheduling && pnpm --filter @campusos/database seed:messaging && pnpm --filter @campusos/database seed:enrollment && pnpm --filter @campusos/database seed:payments && pnpm --filter @campusos/database seed:profile && pnpm --filter @campusos/database seed:iam",
    "db:studio": "pnpm --filter @campusos/database studio",
    "clean": "turbo clean && rm -rf node_modules"
  },
  "devDependencies": {
    "prettier": "^3.3.0",
    "turbo": "^2.1.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@10.33.2"
}
